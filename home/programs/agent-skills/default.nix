{ ... }:

{
  # This directory is imported automatically by home/programs/default.nix.
  # Shared skill assets live under ./shared. Publication is owned by each agent
  # module (home/programs/claude and home/programs/codex), so this module
  # intentionally has no options.
}
