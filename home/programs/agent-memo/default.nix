{ ... }:

{
  # This directory is imported automatically by home/programs/default.nix.
  # memo-shared.ts is published as a sibling symlink under each agent's
  # scripts directory (claude / codex / opencode); this module intentionally
  # has no options.
}
