{ ... }:

{
  # This directory is imported automatically by home/programs/default.nix.
  # Cross-agent assets live under ./skills, ./memo, and ./shared. Publication
  # is owned by each agent module (home/programs/claude, home/programs/codex,
  # and future agents), so this module intentionally has no options.
}
