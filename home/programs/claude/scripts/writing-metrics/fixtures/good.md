# 検証用の良い文書

この作業を完了するには、まず最終確認を通過する必要がある。

- **設計判断**: 辞書は単一の正本から読み込む
- SOURCE `baseline-mixing.ts:10-14` — proseOnly の構造を移植する

Task 3 を実装した。Round 1 の指摘はすべて反映済みだ。

「この task は複雑だ」という文は訳すべき例として引用されている。

処理は入力から検証へ進み、その後保存される（詳細は後述）。
